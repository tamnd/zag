# v0.0.198

Implement _thread module (fixture 198).

## Changes

- `src/vm/thread_mod.zig`: new module with allocate_lock(), LockType,
  start_new_thread(), get_ident(), get_native_id(), exit(), stack_size(),
  error (RuntimeError subclass), TIMEOUT_MAX; acquire() runs pending
  threads before blocking to support lock-based synchronization patterns;
  each thread gets a unique ID different from the main thread.
- `src/vm/interp.zig`: add thread_module, thread_lock_class,
  thread_error_class, thread_mod_pending, thread_mod_next_id,
  thread_mod_current_id fields.
- `tests/fixtures/198_*`: fixture from goipy.
