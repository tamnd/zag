# v0.0.195

Implement sched module (fixture 195).

## Changes

- `src/vm/sched_mod.zig`: new module with scheduler class; enter(),
  enterabs(), cancel(), empty(), run(blocking=True/False), queue
  property; events sorted by (time, priority, sequence); FakeClock
  tests pass with no real sleep.
- `src/vm/dispatch.zig`: make invokeKw pub so sched can call
  scheduled actions with keyword arguments.
- `src/vm/interp.zig`: add sched_module, sched_scheduler_class,
  sched_event_class fields.
- `tests/fixtures/195_*`: fixture from goipy.
