# 0.0.180.1

Patch: align fixture 180 with goipy (was token, now argparse).

- `nargs='?'`, `nargs='*'`, `nargs='+'`, `nargs=N` support
- `action='append'`, `action='count'`, `action='store_const'`
- `set_defaults(**kwargs)` and `get_default(dest)`
- `Namespace.__contains__` for `'x' in ns`
- `parse_known_args(args)` returning `(namespace, extra_list)`
- `add_argument_group(title)` and `add_mutually_exclusive_group()`
- Formatter classes: `HelpFormatter`, `RawDescriptionHelpFormatter`, `RawTextHelpFormatter`, `ArgumentDefaultsHelpFormatter`, `MetavarTypeHelpFormatter`
- `format_usage()` and `format_help()` returning strings
- `--long=value` syntax, `--my-arg` → `my_arg` dest
- `FileType` callable stub
- `ArgumentError` and `ArgumentTypeError` subclassing `Exception`
- Constants: `SUPPRESS = '==SUPPRESS=='`, `REMAINDER = '...'`
