# 0.0.187

Fixture 187: io module extended (goipy 172_io).

- `io.DEFAULT_BUFFER_SIZE`, `io.SEEK_SET`, `io.SEEK_CUR`, `io.SEEK_END` — module constants
- `io.UnsupportedOperation` — exception class inheriting OSError and ValueError
- `io.open()` — delegates to the builtin open
- `StringIO.readable()`, `StringIO.writable()`, `StringIO.seekable()` — return True
- `BytesIO.readline()`, `BytesIO.readlines()`, `BytesIO.writelines()` — new methods
- `BytesIO.readable()`, `BytesIO.writable()`, `BytesIO.seekable()` — return True
- `BytesIO.__iter__` / `__next__` — line-by-line iteration
- File: `seek()`, `tell()`, `truncate()`, `fileno()`, `isatty()` — new methods
- File: `readable()`, `writable()`, `seekable()` — reflect open mode
- File: `readline()`, `writelines()`, `__iter__` / `__next__` — line iteration
- File: `encoding` and `errors` attributes on text-mode files
- File: `read(n)` — supports optional size argument
- `writelines` on StringIO, BytesIO, and file objects now accepts generator expressions
