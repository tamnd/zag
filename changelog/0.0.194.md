# v0.0.194

Implement subprocess module (fixture 194).

## Changes

- `src/vm/subprocess_mod.zig`: full rewrite; run(), Popen, call(),
  check_call(), check_output(), getoutput(), getstatusoutput();
  PIPE/DEVNULL/STDOUT constants; SubprocessError, CalledProcessError,
  TimeoutExpired exception hierarchy; PopenState heap struct stores
  child process pointer on instance dict.
- `src/vm/interp.zig`: add subprocess_completed_proc_class,
  subprocess_error_class, subprocess_timeout_class,
  subprocess_popen_class fields.
- `tests/fixtures/194_*`: fixture from goipy.
