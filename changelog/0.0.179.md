# 0.0.179

Fixture 179: gc module.

- `gc.isenabled`, `gc.enable`, `gc.disable` — GC on/off toggle
- `gc.collect()` — stub returns 0 (no cycle collector)
- `gc.get_objects()` — returns empty list
- `gc.callbacks` — empty list attribute
- `gc.get_freeze_count()` — returns 0
