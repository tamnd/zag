# 0.0.173

Fixture 173: pprint module.

- `pprint.pformat`, `pprint.pprint`, `pprint.pp` — format values with
  pretty-printing, respecting `width`, `indent`, `depth`, `compact`,
  `sort_dicts` options
- `pprint.PrettyPrinter` class with `pformat`, `pprint`, `format`,
  `isreadable`, `isrecursive` methods
- `pprint.saferepr` — safe repr that handles recursion
- `pprint.isreadable`, `pprint.isrecursive` — predicate functions
