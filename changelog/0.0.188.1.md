# v0.0.188.1

Align fixture 188 with goipy: implement `cmd` module.

## Changes

- `src/vm/cmd_mod.zig`: new module implementing `cmd.Cmd` base class
  with `parseline`, `onecmd`, `cmdloop`, `do_help`, `columnize`, hook
  methods (`preloop`, `postloop`, `precmd`, `postcmd`), and default
  class attributes (`prompt`, `identchars`, `ruler`, headers)
- `src/vm/interp.zig`: register `cmd` module; add `cmd_module` field
- `tests/fixtures/188_cmd.*`: fixture from goipy
