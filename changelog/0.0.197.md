# v0.0.197

Implement contextvars module (fixture 197).

## Changes

- `src/vm/contextvars_mod.zig`: new module with ContextVar, Token,
  Context, copy_context(); get/set/reset; Token and set() as context
  managers; Context.run() with context snapshot/restore; mapping
  interface (keys, values, items, __contains__, __getitem__, len);
  Token.MISSING sentinel.
- `src/vm/threading_mod.zig`: add context isolation to
  runPendingThreads — each thread runs with a copy of the context
  at start() time; outer context restored after the thread completes.
- `src/vm/interp.zig`: add contextvars_module, cv_var_class,
  cv_token_class, cv_context_class, cv_missing, cv_context_data,
  cv_context_cvs fields.
- `tests/fixtures/197_*`: fixture from goipy.
