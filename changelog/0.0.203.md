# v0.0.203

Implement selectors module (fixture 203).

## Changes

- `src/vm/selectors_mod.zig`: new module with DefaultSelector (register,
  unregister, modify, get_key, get_map, select, close, __enter__/
  __exit__); SelectorKey class (fileobj, fd, events, data attrs);
  EVENT_READ=1, EVENT_WRITE=2 constants.  select() uses libc poll()
  underneath.  EpollSelector/KqueueSelector not exposed — fixture skips
  them via hasattr.  close() sets _closed flag; subsequent select() raises
  ValueError.
- `src/vm/interp.zig`: add selectors_module, selectors_selector_key_class,
  selectors_default_selector_class fields; register "selectors" in
  getBuiltinModule.
- `tests/fixtures/203_*`: fixture from goipy.
