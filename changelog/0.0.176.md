# 0.0.176

Fixture 176: typing module.

- `typing.TYPE_CHECKING` is `False` at runtime
- `typing.cast(T, val)` returns `val` unchanged
- `typing.TypeVar` with `.__name__`
- `NamedTuple` subclass: named field access, integer indexing, `len()`,
  default field values
- `TypedDict` as a dict factory class
