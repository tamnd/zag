# 0.0.176.1

Patch: align fixture 176 with goipy (was typing, now logging_handlers).

- `RotatingFileHandler(filename, mode, maxBytes, backupCount)` — rotates on size
- `TimedRotatingFileHandler(filename, when, interval, backupCount)` — rotates on time
- `WatchedFileHandler(filename, mode)` — reopens if file replaced
- `BufferingHandler(capacity)` — buffers in memory, flushes at capacity
- `MemoryHandler(capacity, flushLevel, target)` — buffers to target on capacity/level
- `QueueHandler(queue)` — emits via `queue.put_nowait`
- `QueueListener(queue, *handlers)` — `start()/stop()` draining handlers
- Stub handlers: `SocketHandler`, `DatagramHandler`, `SMTPHandler`, `HTTPHandler`
- `SysLogHandler` with `LOG_USER=1` class attr
- Constants: `DEFAULT_TCP_LOGGING_PORT`, `DEFAULT_UDP_LOGGING_PORT`, `SYSLOG_UDP_PORT`
- Added kw_func support to `logging.FileHandler`, `BufferingHandler`, `MemoryHandler`
