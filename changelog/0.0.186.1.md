# 0.0.186.1

Patch: align fixture 186 with goipy (was time, now curses.ascii).

- `curses.ascii.toascii(c)` — mask to 7-bit ASCII
- `curses.ascii.unctrl(c)` — printable representation (`^X` for control, `^?` for DEL)
- All 13 classification functions: `isascii`, `isdigit`, `isalpha`, `isupper`, `islower`, `isalnum`, `isspace`, `isblank`, `iscntrl`, `isprint`, `ispunct`, `isgraph`, `isxdigit`
- Conversion functions: `toupper`, `tolower`, `ctrl`, `alt`, `ascii`
- 35 named constants: NUL through DEL, SP, EOF
