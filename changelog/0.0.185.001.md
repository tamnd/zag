# 0.0.185.1

Patch: align fixture 185 with goipy (was platform, now curses.textpad).

- `curses.textpad.Textbox` — text editing widget with buffer/cursor model
- `edit(validate=...)` — keyword argument support for validate callback
- `gather()` — return buffer contents as string
- `do_command(ch)` — handle control keys and printable characters
- `rectangle()` — stub for drawing rectangle around window
