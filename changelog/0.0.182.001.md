# 0.0.182.1

Patch: align fixture 182 with goipy (was argparse, now getpass).

- `getpass.getpass(prompt=...)` — keyword argument support, non-interactive stub returns ""
- `getpass.GetPassWarning` — instantiable warning class exposed as type
