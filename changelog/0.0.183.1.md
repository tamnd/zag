# 0.0.183.1

Patch: align fixture 183 with goipy (was abc, now fileinput).

- `fileinput.input(files=..., mode=...)` — multi-file line iterator
- `fileinput.filename()`, `fileinput.fileno()`, `fileinput.lineno()`, `fileinput.filelineno()`
- `fileinput.isfirstline()`, `fileinput.isstdin()`, `fileinput.nextfile()`
- `fileinput.close()` — close current iteration
- `FileInput` class with `__iter__`/`__next__` for lazy iteration
- Fixed GET_ITER opcode to push custom iterators lazily (preserving state between iterations)
- `next()` builtin now handles instances with `__next__` method
