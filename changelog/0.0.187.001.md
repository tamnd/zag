# 0.0.187.1

Patch: align fixture 187 with goipy (was io, now curses.panel).

- `curses.panel.new_panel(win)` — create panel on top of stack
- `curses.panel.top_panel()` — return topmost visible panel
- `curses.panel.bottom_panel()` — return bottommost visible panel
- `curses.panel.update_panels()` — no-op stub
- Panel stack is per-interpreter (no global state leak between tests)
- `panel.top()`, `panel.bottom()` — reorder within stack
- `panel.above()`, `panel.below()` — navigate stack
- `panel.hide()`, `panel.show()`, `panel.hidden()` — visibility control
- `panel.window()` — return associated window
- `panel.replace(win)` — swap associated window
- `panel.set_userptr(ptr)`, `panel.userptr()` — user data attachment
- `panel.move()` — stub
