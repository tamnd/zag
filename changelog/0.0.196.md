# v0.0.196

Implement queue module (fixture 196).

## Changes

- `src/vm/queue_mod.zig`: new module with Queue (FIFO), LifoQueue,
  PriorityQueue (min-heap), SimpleQueue; put/get with block=False
  and timeout kwargs; task_done/join; shutdown(immediate=); Empty,
  Full, ShutDown exceptions (subclass Exception).
- `src/vm/interp.zig`: add queue_module and related class fields.
- `tests/fixtures/196_*`: fixture from goipy.
