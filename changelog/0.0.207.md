# v0.0.207

Extend json module to pass fixture 207.

## Changes

- `src/vm/json_mod.zig`: rewrite to add allow_nan (NaN/Infinity output),
  ensure_ascii=False (raw UTF-8), skipkeys, default callback, object_hook,
  parse_float/parse_int for loads; loads now accepts bytes/bytearray; json.dump
  and json.load (file-like objects); JSONDecodeError with doc/pos/lineno/colno
  attributes inheriting from ValueError; JSONEncoder class with encode();
  JSONDecoder class with decode() and raw_decode().
- `tests/fixtures/207_*`: fixture from goipy.
