# 0.0.182

Fixture 182: argparse module.

- `argparse.ArgumentParser` — create a parser with `description`
- `add_argument('--name', type=..., default=...)` — register optional args
- `parse_args([])` — parse an empty argv, returning defaults
- `parse_args(['--foo', '42', ...])` — parse explicit values with type coercion
- `argparse.Namespace(x=1, y=2)` — create a namespace directly
