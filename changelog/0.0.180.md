# 0.0.180

Fixture 180: token module.

- `token.NAME`, `NUMBER`, `STRING`, `NEWLINE`, `INDENT`, `DEDENT`,
  `ENDMARKER` — lexer token type constants
- `token.LPAR`, `RPAR`, `COLON`, `COMMA`, `DOT` — punctuation constants
- `token.tok_name` — dict mapping token id to name
- `token.ISEOF(tok)` — True only for ENDMARKER (0)
