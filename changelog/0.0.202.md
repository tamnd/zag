# v0.0.202

Implement select module (fixture 202).

## Changes

- `src/vm/select_mod.zig`: new module with select(), poll class (register,
  unregister, modify, poll); select.error alias for OSError; constants
  POLLIN/POLLOUT/POLLERR/POLLHUP/POLLNVAL/POLLPRI; epoll and kqueue not
  exposed (fixture skips them via hasattr checks).  Both select() and
  poll.poll() use libc poll() under the hood.
- `src/vm/interp.zig`: add select_module, select_poll_class fields;
  register "select" in getBuiltinModule.
- `tests/fixtures/202_*`: fixture from goipy.
