# v0.0.212

Implement quopri module to pass fixture 212.

## Changes

- `src/vm/quopri_mod.zig`: new module with encodestring (quotetabs, header
  kwargs) and decodestring (header kwarg); handles =XX encoding/decoding,
  soft line breaks (=\n), and header mode (space ↔ underscore).
- `src/vm/interp.zig`: add quopri_module field; register "quopri" in
  getBuiltinModule.
- `tests/fixtures/212_*`: fixture from goipy.
