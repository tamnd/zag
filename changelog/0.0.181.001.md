# 0.0.181.1

Patch: align fixture 181 with goipy (was ast, now optparse).

- `optparse.OptionParser` with `add_option`, `parse_args`
- Short and long options: `-v`, `--verbose`, combined `-abc`
- `action='store'`, `store_true`, `store_false`, `store_const`, `append`, `count`
- `type='int'`, `type='float'`, `type='string'`
- `dest`, `default`, `const`, `nargs`, `metavar` parameters
- `add_option_group(title)` returning group that delegates to parser
- `set_defaults(**kwargs)`, `get_default(dest)`, `set_usage(usage)`
- `format_usage()`, `format_help()` returning strings
- `OptionGroup` with group title
- `OptionError`, `OptionValueError` subclassing `Exception`
- `Values` namespace object with `ensure_value(name, value)`
- `SUPPRESS_HELP`, `SUPPRESS_USAGE` constants
- `make_option` alias for `Option`
