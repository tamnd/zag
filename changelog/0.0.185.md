# 0.0.185

Fixture 185: platform module (goipy 177_platform).

- `platform.uname()` — returns object with system, node, release, version,
  machine, processor attributes
- `platform.platform()` — returns non-empty string
- `platform.win32_ver()` — 4-tuple
- `platform.mac_ver()` — 3-tuple
- `platform.python_build()` — 2-tuple
- `platform.python_compiler()`, `python_branch()`, `python_revision()` — strings
