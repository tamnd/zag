# 0.0.183

Fixture 183: abc module.

- `ABC` base class and `@abstractmethod` decorator
- Subclass with concrete implementation can be instantiated
- Abstract class raises `TypeError` on direct instantiation
- `__abstractmethods__` is now a `frozenset` (was a list)
- `isinstance` and `issubclass` work correctly with ABC hierarchies
