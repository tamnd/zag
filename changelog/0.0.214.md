# v0.0.214

Fixture 214: extended `html.parser.HTMLParser` — processing instructions, CDATA/unknown_decl, entity/charref callbacks, `convert_charrefs` kwarg, `getpos()`, and incremental feed buffering.

## Changes

- `src/vm/html_mod.zig`:
  - `ParserState` gains `line`/`col` fields for position tracking
  - `parserGetposFn`: new `getpos()` method returning `(line, col)` tuple
  - `emitText` helper: when `convert_charrefs=True` (default), unescape entities in text data; when `False`, emit `handle_entityref`/`handle_charref` callbacks for `&name;` and `&#NNN;` patterns
  - `parserFeed` rewritten: uses `state.buf` for incremental buffering, handles `<?...?>` (PI), `<![...]>` (unknown_decl/CDATA), all existing token types; leaves incomplete tokens in buffer across feed() calls
  - New base class no-ops: `handle_pi`, `unknown_decl`, `handle_entityref`, `handle_charref`
  - `buildParser`: registers all new methods
- `tests/fixtures/fixtures.zig`: added `214_html_parser`
- `tests/fixtures/214_html_parser.*`: fixture files

All 214 fixtures pass. All 5 cross-compilation targets clean.
