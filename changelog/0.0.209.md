# v0.0.209

Add `mimetypes` module (fixture 209).

Implements `guess_type`, `guess_all_extensions`, `guess_extension`,
`add_type`, `init`, `read_mime_types`, and the `MimeTypes` class.
Module-level `suffix_map`, `encodings_map`, and `types_map` are
populated at build time with 100+ common MIME entries.
