# 0.0.175.1

Patch: align fixture 175 with goipy (was uuid, now logging_config).

- `logging.config.dictConfig(cfg)` — full dict-based config: FileHandler, NullHandler, Formatter, Filter, propagate, incremental flag
- `logging.config.fileConfig(path, ...)` — INI-based config for root and named loggers
- `logging.config.listen(port)` / `stopListening()` — stubs returning a thread-like object
- Hierarchical log propagation: messages walk up the logger name hierarchy (dot-separated) before reaching root
- `propagate` flag on LoggerState persisted through dictConfig/fileConfig
- `getLogger('')` maps to the root logger
