# 0.0.178

Fixture 178: inspect module.

- `inspect.isfunction`, `isclass`, `isbuiltin`, `ismodule`, `ismethod`,
  `isroutine` — type-checking predicates
- `inspect.getmembers(obj)` — sorted list of `(name, value)` tuples
